<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lu.admin.modules.user.mapper.LoginLogMapper">
    <select id="getByPage" resultType="com.lu.admin.modules.user.dto.LoginLogDto">
        select t.*, t1.nick_name
        from login_log t
        inner join `user` t1 on t.user_id = t1.id and t1.deleted = 0
        where t.deleted = 0
        <if test="params.nickName != null and params.nickName != ''">
            and t1.nick_name like CONCAt('%', #{params.nickName,jdbcType=VARCHAR}, '%')
        </if>
        <if test="params.startTime != null and params.endTime != null">
            <![CDATA[and t.create_time >= #{params.startTime} and t.create_time <= #{params.endTime} ]]>
        </if>
    </select>
</mapper>
